[package]
name = "metis-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core types, cryptographic primitives, capability tokens, audit ledger, and IPC wire protocol for Metis"

[dependencies]
# Pure Rust std only. Zero third-party dependencies.

[lints]
workspace = true
